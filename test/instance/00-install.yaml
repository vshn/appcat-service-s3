apiVersion: appcat.vshn.io/v1
kind: ObjectBucket
metadata:
  annotations:
    kuttl: '00-install'
  name: bucket
  namespace: default
spec:
  parameters:
    bucketName: my-provider-test-bucket-from-claim
    region: rma
  writeConnectionSecretToRef:
    name: credentials
